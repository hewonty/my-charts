apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imgr_svc_chart.fullname" . }}-test-connection"
  labels:
    {{- include "imgr_svc_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imgr_svc_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
